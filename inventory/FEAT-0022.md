---
id: FEAT-0022
status: implemented
tags: [packaging, skill]
created: "2026-07-06T05:13:05Z"
updated: "2026-07-06T05:13:05Z"
references:
  CHORE-0037: Fix flake.nix workspace version — nix run is broken
  CHORE-0038: Write the 0.12.0 changelog and deprecate the orphaned opys-core crate
  CHORE-0039: Give the opys crate a crates.io README and complete crate metadata
  CHORE-0040: Docs sweep before the announcement
  TASK-0047: 'Announcement assets: demo recording and comparison table'
---

# Multi-agent skill and plugin packaging

The repo doubles as a plugin carrying the tool-agnostic `opys` skill (`skills/opys/`, with the normative format spec): Claude Code and Codex plugin manifests, a Gemini extension, opencode config, and a pi extension all point at the same folder, with a version-sync check gating CI. The CLI itself stays universal — any agent that can run a shell command can use it.

## Test plan
- [x] the canonical rule generates every editor's file — `cli::agent_rules_generates_editor_files`
- [ ] the skill folder is self-contained when copied (no dead relative links) — uncovered (known hole, see linked chore)
