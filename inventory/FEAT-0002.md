---
id: FEAT-0002
status: implemented
tags: [core, ids]
created: "2026-07-06T05:13:05Z"
updated: "2026-07-06T05:13:05Z"
references:
  BUG-0023: ~~close does not reserve an unreferenced doc's id~~
  BUG-0026: ~~Corrupt _retired.md is silently read as an empty ledger~~
  BUG-0028: ~~retire leaves inbound references dangling and the corpus failing verify~~
---

# Global never-reuse ID sequence and retired ledger

IDs are drawn from a single global, monotonically increasing sequence shared by every type. A number is never reused: allocation takes the max over live docs, the `_retired.md` ledger, and every relation map (struck or not). `retire` and `close` append to the sorted ledger (the reservation of record — tombstones in relation maps are presentation); `cleanup` ledger-reserves any id whose last tombstone it strips; `verify` rejects reuse and duplicate numbers. An unreadable ledger (markdown or legacy plaintext) is an error that blocks allocation, reservation, and flush — never an empty read.

## Test plan
- [x] ids share one global increasing sequence across prefixes — `cli::ids_share_one_global_increasing_sequence`
- [x] retire deletes the file and reserves the id — `cli::retire_deletes_and_reserves_id`
- [x] the retired ledger stays sorted by number — `cli::retired_ledger_is_sorted_by_number`
- [x] allocation covers docs, relation maps, and the ledger — `store::next_id_covers_docs_relations_and_ledger`
- [x] bulk retire reserves every id in the batch — `cli::bulk_retire_reserves_every_id`
- [x] closing an *unreferenced* doc reserves its id — `cli::close_unreferenced_doc_reserves_id`
- [x] a closed doc's carried tombstones stay reserved — `cli::close_reserves_tombstones_carried_by_the_closed_doc`
- [x] cleanup reserves the ids of stripped tombstones — `cli::cleanup_reserves_stripped_tombstone_ids`
- [x] an unparseable `_retired.md` is an error, not an empty ledger — `cli::corrupt_ledger_blocks_allocation_and_stays_untouched`
- [x] verify flags a corrupt ledger as a content problem — `cli::verify_flags_corrupt_ledger`
