---
id: FEAT-0042
status: wontfix
tags: [agents, interface]
created: "2026-07-06T16:10:52Z"
updated: "2026-07-06T16:10:52Z"
wontfix_reason: "Skill + CLI is the interface: every coding agent has a shell, and 'any agent that can run a shell command can use opys' is a differentiator. MCP would add install/auth friction, permanent tool-schema token overhead, and a second surface to keep in sync. Revisit if non-shell hosts (claude.ai chat, ChatGPT connectors) show real demand — the opys-engine/Backend split makes a thin stdio server cheap to add later."
---

# MCP server interface

An MCP server exposing the inventory as tools, considered as an alternative agent interface alongside the skill and the CLI.

## Test plan
- [ ] stdio MCP server over opys-engine — not planned; see wontfix reason
