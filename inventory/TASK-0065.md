---
id: TASK-0065
status: todo
created: "2026-08-23T16:39:47-04:00"
updated: "2026-08-23T18:26:48-04:00"
references:
  FEAT-0020: Terminal UI board
  ADR-0050: Retire the TUI; the web UI over the server is the human surface
---

# Remove the opys-tui crate

Execute [ADR-0050 — Retire the TUI; the web UI over the server is the human surface](ADR-0050.md): cut the TUI from the workspace before the announcement.
Git history preserves the code; the ssh niche is covered by `watch opys list`
/ `opys query`.

## Tasks
- [ ] remove the `opys-tui` crate, the workspace member, and the `tui` feature/`Command::Tui` gating in the `opys` binary
- [ ] drop TUI mentions from README, AGENTS.md, CI (`--all-features` matrix), and release-plz config
- [ ] recheck the MSRV floor without ratatui/instability (`msrv` devShell command) and lower `rust-version` if the dep tree allows
- [ ] mark the crates.io `opys-tui` crate deprecated/yanked-note alongside the [CHORE-0038 — Write the 0.12.0 changelog and deprecate the orphaned opys-core crate](CHORE-0038.md) opys-core deprecation
- [ ] `cargo test --all` + clippy green after removal

## Progress
- Filed as the execution task for [ADR-0050 — Retire the TUI; the web UI over the server is the human surface](ADR-0050.md); [FEAT-0020 — Terminal UI board](FEAT-0020.md) goes to wontfix in the
  same change.
