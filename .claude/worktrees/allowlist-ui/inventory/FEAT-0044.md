---
id: FEAT-0044
status: wontfix
tags: [backend, storage]
created: "2026-07-06T16:10:52Z"
updated: "2026-07-06T16:10:52Z"
wontfix_reason: "Markdown-over-S3 loses git, and git is half the value: history, branching, PR review of knowledge changes, merge semantics, renumber. It also makes the locking story harder, and the user it serves — shared state without a repo — is drifting toward a hosted tracker, the explicitly declined customer. The Backend trait already pays for itself as a layering/testability seam; a second backend doubles the invariant surface while the first still has open holes. Revisit only when a real user brings a real use case."
---

# S3 storage backend

A second Backend implementation storing the document corpus in an S3 bucket instead of the local filesystem.

## Test plan
- [ ] markdown-over-s3 backend — not planned; see wontfix reason
